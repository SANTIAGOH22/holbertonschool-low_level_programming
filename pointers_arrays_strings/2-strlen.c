#include "main.h"
#include <stdio.h>

/**
 * _strlen - function
 * @s:this is char
 * Description: Write a function that returns the length of a string.
 * Return: Always 0.
 */
int _strlen(char *s)
{
int l, count = 0;
for (l = 0; s[i] != '\0'; ++i)
{
count++;
}
return (count);
