#include <stdio.h>

/**
 * main -a C "ith proper grammar,but the outcome is a piece of art,
 *
 * Return: 0 (Sucess);
 */
int main(void)
{
puts("but the outcome is a piece of artbut the outcome is a piece of art,\n");
return (0);
}
